---

repo: limithit/NginxExecute
summary: NGINX Execute module
description: |
  The NginxExecute module executes the shell command through GET and
  POST to display the result.
soname: ngx_http_execute_module